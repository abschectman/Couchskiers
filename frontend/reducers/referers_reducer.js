// import { SHOW_USER } from "../actions/user_actions";

// const referersReducer = (state = {}, action) => {
//   Object.freeze(state);
//   switch (action.type) {
//     case SHOW_USER: {
//       let newState = state;
//       return Object.assign({}, newState, action.referers);
//     }
//     default:
//       return state;
//   }
// };

// export default referersReducer;
